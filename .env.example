# Copy to .env and set real values before deploying to production.
PORT=3000
# Generate a long random string, e.g. in Node:  require("crypto").randomBytes(48).toString("hex")
JWT_SECRET=change-me-to-a-long-random-secret
NODE_ENV=production
